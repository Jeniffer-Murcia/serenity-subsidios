environments{

    default {
        webdriver {
          base.url = "https://stg-subsidionomina.awsbanbogota.com/login"
        }
      }

    stg{
        webdriver{
            base.url = "https://stg-subsidionomina.awsbanbogota.com/login"
        }
    }

    qa {
        webdriver{
            base.url = "https://qa-subsidionomina.awsbanbogota.com/login"
        }
    }

    prod {
        webdriver{
            base.url = "https://subsidiobogota.com/login"
        }
    }
}

serenity {
    take.screenshots = FOR_EACH_ACTION
    # Cambia a FOR_FAILURES si quieres solo en fallos
}

webdriver {
  driver = chrome
  capabilities {
    browserName = "chrome"
    acceptInsecureCerts = true
    "goog:chromeOptions" {
      args = ["remote-allow-origins=*","test-type", "no-sandbox", "ignore-certificate-errors", "--window-size=1000,800",
        "incognito", "disable-infobars", "disable-gpu", "disable-default-apps", "disable-popup-blocking",
        "disable-dev-shm-usage", "disable-extensions", "disable-web-security", "disable-translate", "disable-logging"]
    }
  }
}